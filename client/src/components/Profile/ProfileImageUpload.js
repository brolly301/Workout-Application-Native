import { StyleSheet, Text, View } from "react-native";
import React from "react";

const ProfileImageUpload = () => {
  return (
    <View>
      <Text>ProfileImageUpload</Text>
    </View>
  );
};

export default ProfileImageUpload;

const styles = StyleSheet.create({});
